/*const changeFilter = () => {
	divImg.style.filter = {
		brightness: brightness.value,
		opacity: opacity.value,
		contrast: `${contrast.value}%`
		blur: `${blurSlider.value}px)`
	greyscale(${grayscale.value}%`
	sepia(${sepia.value`
	hue-rotate(${hue.value}deg)
	saturate(${saturate.value}%)
	invert(${invert.value}%)
	}
};




const changeFilter = ()=>{
	const filter = 	`brightness(${brightness.value}) opacity(${opacity.value}) 
	contrast(${contrast.value}%) blur(${blurSlider.value}px) grayscale(${grayscale.value}%) 
	sepia(${sepia.value}%) hue-rotate(${hue.value}deg) saturate(${saturate.value}%) invert(${invert.value}%)`;
	console.log(filter)
	divImg.style.filter = filter
};

brightness.addEventListener("change", changeFilter);
opacity.addEventListener("change", changeFilter);
contrast.addEventListener("change", changeFilter);
blurSlider.addEventListener("change", changeFilter);

*/